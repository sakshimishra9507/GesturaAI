# Testing
Run `pytest`.

Test webcam availability, landmark extraction, model-not-trained behavior, prediction accuracy, class balance, per-class metrics, cross-user validation, FPS, latency, CPU and memory. Report measured results from your own experiments.
