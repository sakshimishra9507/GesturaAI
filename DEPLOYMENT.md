# Deployment
For local demo: `python app.py`.
Do not expose Flask's development server directly to the internet. Production should use a production WSGI server, HTTPS, authentication, secure secrets, rate limiting, monitoring and model versioning. Possible edge targets include Raspberry Pi, Jetson and Intel NUC.
